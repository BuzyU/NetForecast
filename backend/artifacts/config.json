{
  "window": 6,
  "features": [
    "flow_duration",
    "tot_fwd_pkts",
    "tot_bwd_pkts",
    "fwd_pkt_len_mean",
    "bwd_pkt_len_mean",
    "flow_bytes_s",
    "flow_pkts_s",
    "flow_iat_mean",
    "flow_iat_std",
    "fwd_iat_mean",
    "bwd_iat_mean",
    "syn_flag_cnt",
    "ack_flag_cnt",
    "fin_flag_cnt",
    "rst_flag_cnt",
    "psh_flag_cnt",
    "urg_flag_cnt",
    "down_up_ratio",
    "pkt_size_avg",
    "ttl_variance",
    "tcp_win_size",
    "retransmit_cnt"
  ],
  "stages": [
    "Benign",
    "Reconnaissance",
    "Initial Access",
    "Lateral Movement",
    "C2",
    "Exfiltration"
  ],
  "hidden_size": 256,
  "num_layers": 2,
  "lstm_dropout": 0.25,
  "stage_logit_bias": {
    "Benign": 1.0,
    "Reconnaissance": 0.25,
    "Initial Access": 0.0,
    "Lateral Movement": 0.0,
    "C2": 0.5,
    "Exfiltration": 0.0
  },
  "provenance": {
    "trained_on": "real_flows.csv",
    "train_sessions": 1673,
    "val_sessions": 239,
    "test_sessions": 478,
    "train_rows": 236272,
    "val_rows": 33734,
    "test_rows": 64642,
    "git_commit": "60e06ef",
    "leakage_fix": "session-level 3-way train/val/test split, scaler fit on train only, checkpoint selection uses val (never test) -- test set is byte-identical across every model version in this project's history (see three_way_split() docstring)",
    "synthetic_augmentation": {
      "stages": [
        "Exfiltration"
      ],
      "sessions_per_stage": 300,
      "note": "train-only synthetic oversampling from calibrated per-stage feature profiles; test set is 100% real flows (CIC-IDS2017 and, if present, CIC-IDS2018), untouched by synthetic augmentation"
    },
    "class_weight_max": 6.0,
    "stage_loss": "focal",
    "focal_gamma": 2.0,
    "best_val_f1": 0.8617,
    "optimization": "AdamW + CosineAnnealingLR + ClassWeighting",
    "initial_access_resession_fix": "data/fix_initial_access_sessions.py adds 2,180 real CIC-IDS2017 web-attack rows re-chunked into pure attack-only sessions, alongside (not replacing) the original mixed-session rows -- see docs/model_card.md section 5 for the root-cause analysis",
    "stage_logit_bias_calibration": "per-class additive bias on stage logits, coordinate-ascent search on val split only (test set never touched) to maximize macro-F1; test-set effect after the resession fix: macro-F1 0.6514->0.6901, Initial Access F1 0.437->0.530 (P 0.317->0.535)"
  }
}